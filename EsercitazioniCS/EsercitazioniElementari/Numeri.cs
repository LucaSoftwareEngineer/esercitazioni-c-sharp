using System;
using System.Collections.Generic;
using System.Text;

namespace EsercitazioniCS.EsercitazioniElementari
{
    internal class Numeri
    {
        public Numeri() {
            //esecitazioni numeri
            Math.Pow(2, 3);
            Math.Sqrt(16);
            Math.Min(5, 10);
            Math.Max(5, 10);
        }
    }
}
